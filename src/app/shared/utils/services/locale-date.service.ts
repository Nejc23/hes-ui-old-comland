import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class LocaleDateService {
  constructor() {}

  dateDefault(date: string) {
    // TODO: complete this
    // return new DatePipe().transform(date);
  }
}
